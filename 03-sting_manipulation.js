//Strings
      /*
      .startsWith(searchString) and .endsWith(searchString)
      .includes(searchString)
      .replace(searchValue, newValue)
      .join(separator)
      .split(separator)
      .trim()
      .length
      */